# ===============================================
# TEST CONFIGURATION
# ===============================================

# H2 In-Memory Database
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=false

# Disable Flyway for tests
spring.flyway.enabled=false

# JWT Configuration (Test values - Base64-encoded 512-bit key for HS512)
jwt.secret=CTl/xK0434/aLcxx5A+Hn6lIEG5vxiqdcu1Wh8yT0I5yRzeZVtlsON6u83FSWe6ZuIdTHznvuSuEutP8GE30Fg==
jwt.expiration=3600000
jwt.refresh-expiration=86400000

# Disable Kafka for unit tests
spring.kafka.bootstrap-servers=localhost:9092

# Disable Redis for unit tests  
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Logging
logging.level.com.RRBank=DEBUG
logging.level.org.springframework.security=WARN

# CORS (Test)
cors.allowed-origins=http://localhost:3000
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true
